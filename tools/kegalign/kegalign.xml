<tool id="kegalign" name="KegAlign" version="@KEGALIGN_VERSION@+galaxy@KEGALIGN_SUFFIX@" profile="@PROFILE@">
    <description>A Scalable GPU System for Pairwise Whole Genome Alignments based on LASTZ's seed-filter-extend paradigm</description>
    <macros>
        <!-- ▶ THIS TOOL'S SUFFIX LIVES HERE, NOT IN macros.xml. Shared files are what
             couple the three tools' versions together; a token only this tool uses has
             no business in one. Bump it here and the other two stay where they are. -->
        <token name="@KEGALIGN_VERSION@">0.3.3</token>
        <token name="@KEGALIGN_SUFFIX@">0</token>
        <import>macros.xml</import>
        <import>sequence_options.xml</import>
        <import>scoring_options.xml</import>
        <import>seeding_options.xml</import>
        <import>ungapped_extension_options.xml</import>
        <import>gapped_extension_options.xml</import>
        <import>interpolation_options.xml</import>
        <import>output_options.xml</import>
        <import>system_options.xml</import>
        <import>alignment_type_option.xml</import>
    </macros>
    <expand macro="edam_ontology"/>
    <expand macro="kegalign_requirements"/>
    <command detect_errors="exit_code"><![CDATA[
## Convert input sequences to 2bit -------------------------------------

## ⚠ NO PROCESS SUBSTITUTION HERE. `faToTwoBit <(gzip -cdfq ...)` is a bashism, and the
## job script is not guaranteed to run under bash: on usegalaxy.org's vgp destination the
## container's /bin/sh rejects it with `syntax error: unexpected "("` and the tool dies at
## exit 2 before reading a single base. The wrapper carried that line unchanged from
## 2024-08-29 and every container tolerated it until this one, so this is a latent bashism
## finally meeting a POSIX shell -- not a regression in the aligner.
## Decompressed to a real file rather than a FIFO or /dev/stdin so that nothing depends on
## faToTwoBit accepting a non-seekable input, and removed immediately after conversion so
## the uncompressed copies do not sit beside the 2bit files for the life of the job.

mkdir -p "\$(pwd)/work" &&
gzip -cdfq '$target' > "\$(pwd)/work/ref.fa" &&
faToTwoBit "\$(pwd)/work/ref.fa" "\$(pwd)/work/ref.2bit" &&
gzip -cdfq '$query' > "\$(pwd)/work/query.fa" &&
faToTwoBit "\$(pwd)/work/query.fa" "\$(pwd)/work/query.2bit" &&
rm -f "\$(pwd)/work/ref.fa" "\$(pwd)/work/query.fa" &&

## The scripts come from the kegalign package, which installs them to its bin
## directory alongside the aligner -- this tool no longer carries copies of them.
## `--tool_directory` still has to name that directory, because runner.py locates
## diagonal_partition.py through it and package_output.py locates lastz-cmd.ini.
KEGALIGN_BIN=\$(dirname "\$(command -v runner.py)") &&
test -f "\$KEGALIGN_BIN/runner.py" &&

## Run KegAlign --------------------------------------------------------

## explicitly calling python to bypass a pulsar bug
## https://github.com/galaxyproject/pulsar/issues/341
python "\$KEGALIGN_BIN/runner.py"
    --output-type tarball
    --output-file '$kegalign_output'
    --diagonal-partition
    --num-cpu \${GALAXY_SLOTS:-2}
    --tool_directory "\$KEGALIGN_BIN"
    '$target'
    '$query'

## Sequence Options ----------------------------------------------------

    --strand '$sequence_options.strand_selector'

## Scoring Options -----------------------------------------------------

#set $scoring_pathname = str($scoring_options.scoring)
#if $scoring_pathname != "None":
    --scoring '$scoring_pathname'
#end if
## ⚠ THE REWARD/PENALTY PAIR LIVES INSIDE THE `ambiguous_params` CONDITIONAL, so the path carries
## that level too. Addressed one level too shallow, Cheetah fails only when a user picks something
## other than the default -- which short-circuits this whole block, so it never showed up.
#if str($scoring_options.ambiguous_selector) != "x"
    #if str($scoring_options.ambiguous_params.set_ambiguous_params_selector) == "true"
        #set $argument_value = ','.join([str($scoring_options.ambiguous_selector), str($scoring_options.ambiguous_params.ambiguous_reward), str($scoring_options.ambiguous_params.ambiguous_penalty)])
    --ambiguous '$argument_value'
    #else
    --ambiguous '$scoring_options.ambiguous_selector'
    #end if
#end if

## Seeding Options -----------------------------------------------------

#if str($seeding_options.seed.seed_selector) == "custom"
    --seed '$seeding_options.seed.custom_seed'
#else
    --seed '$seeding_options.seed.seed_selector'
#end if
    --step '$seeding_options.step'
#if str($seeding_options.notransition) == "true"
    --notransition
#end if

## Ungapped Extension Options ------------------------------------------

    --xdrop '$ungapped_extension_options.xdrop'
    --hspthresh '$ungapped_extension_options.hspthresh'
#if str($ungapped_extension_options.noentropy) == "true"
    --noentropy
#end if

## Gapped Extension Options --------------------------------------------

    --ydrop '$gapped_extension_options.ydrop'
#if str($gapped_extension_options.gappedthresh) != ""
    --gappedthresh '$gapped_extension_options.gappedthresh'
#end if
#if str($gapped_extension_options.notrivial) == "true"
    --notrivial
#end if

## Interpolation Options -----------------------------------------------

#if int($interpolation_options.inner) > 0
    --inner='$interpolation_options.inner'
#end if

## Output Options ------------------------------------------------------

## Resolve the selector to the LASTZ format ONCE. It is needed twice -- here for lastz, and below
## for package_output.py, which records it in the tarball so batched_lastz can assign the Galaxy
## datatype. Passing the SELECTOR there instead of this value is what typed SAM output as `tabular`:
## batched_lastz's table is keyed on lastz format names, and `bam` is not one of them.
#if str($output_options.format.format_selector) == "bam"
    #set $lastz_format = str($output_options.format.bam_options)
#else if str($output_options.format.format_selector) == "general_def"
    #set $lastz_format = "general-"
#else if str($output_options.format.format_selector) == "general_full"
    #set $lastz_format = "general-:%s" % str($output_options.format.fields)
#else if str($output_options.format.format_selector) == "maf"
    #set $lastz_format = str($output_options.format.maf_type)
#else if str($output_options.format.format_selector) == "axt"
    #set $lastz_format = str($output_options.format.axt_type)
#else if str($output_options.format.format_selector) == "lav"
    #set $lastz_format = str($output_options.format.lav_type)
#else if str($output_options.format.format_selector) == "blastn"
    #set $lastz_format = "BLASTN-"
#else if str($output_options.format.format_selector) == "differences"
    #set $lastz_format = "differences"
#end if
    --format '$lastz_format'
## todo :: rplot, bam
##  --action:target=multiple
##  $output_format.rplot
##  .if str( $output_format.out.format ) == "bam":
##      | samtools sort -@\${GALAXY_SLOTS:-2} -T "\${TMPDIR:-.}" -O bam -o '${output}'
##  .else:
##      > '${output}'
##  .end if
##  .if $output_format.rplot:
##      &&
##      Rscript $r_plot > /dev/null 2>&1
##  .end if

## System Options ------------------------------------------------------

    --wga_chunk_size '$system_options.wga_chunk_size'
    --lastz_interval_size '$system_options.lastz_interval_size'
    --seq_block_size '$system_options.seq_block_size'
    --num_gpu '$system_options.num_gpu'
#if str($system_options.debug) == "true"
    --debug
#end if

## Package Output ------------------------------------------------------

&&
#if str($bundle_selector) == "collection"
## Growler: group the diagonal-partition splits into pair files -- one gzipped segments file per
## (target, query) chromosome pair, both strands, plus-before-minus as the lastz manual requires.
## No tarball, no commands.json, no format.txt: everything the grouping needs (target, query,
## strand) is in columns 1, 4 and 7 of the segment lines themselves.
##
## ⚠ `--format` above still selects what KegAlign writes into its command lines, but nothing
## reads them on this path -- the output format is chosen downstream, on Growler LASTZ.
python "\$KEGALIGN_BIN/build_pairs.py"
    --segments-dir .
    --out pairs
#else
python "\$KEGALIGN_BIN/package_output.py"
    --tool_directory "\$KEGALIGN_BIN"
    --output_format '$lastz_format'
#if str($system_options.debug) == "true"
    --debug
#end if
#end if

    ]]></command>
    <inputs>
        <param name="target" type="data" format="fasta,fasta.gz" label="Target sequence file in FASTA format"/>
        <param name="query" type="data" format="fasta,fasta.gz" label="Query sequence file in FASTA format"/>
        <expand macro="sequence_options"/>
        <expand macro="scoring_options"/>
        <expand macro="seeding_options"/>
        <expand macro="ungapped_extension_options"/>
        <expand macro="gapped_extension_options"/>
        <expand macro="interpolation_options"/>
        <expand macro="output_options"/>
        <expand macro="system_options"/>
        <param name="bundle_selector" type="select" label="Output shape"
               help="Tarball is the original two-step form, consumed by Batched LASTZ. Chromosome pairs emit one element per pair for Growler, which maps LASTZ over them without ever packing or unpacking an archive.">
            <option value="tarball" selected="true">A single tarball (data_package.tgz)</option>
            <option value="collection">A collection of pair files, one per chromosome pair</option>
        </param>
        <section name="developer" title="Developer Options" expanded="false">
            <expand macro="alignment_type_option"/>
        </section>
    </inputs>
    <outputs>
        <data name="kegalign_output" format="tgz" from_work_dir="data_package.tgz" label="KegAlign on ${on_string}">
            <filter>bundle_selector == "tarball"</filter>
        </data>
        <!-- ⚠ Discovery is ASCII-ordered and the elements arrive hidden. The identifier is
             `<target>__<query>`: two underscores, because a single one occurs inside real
             sequence names and downstream regrouping matches these identifiers with a regex. -->
        <collection name="pairs" type="list" label="KegAlign chromosome pairs on ${on_string}">
            <filter>bundle_selector == "collection"</filter>
            <discover_datasets pattern="(?P&lt;designation&gt;.+)\.segments\.gz" directory="pairs" format="tabular.gz" visible="false"/>
        </collection>
        <!-- The 2bits are emitted as their own datasets rather than copied into every pair file: at
             210 MB each they are 2.9% of a bundle, but duplicated across 100 elements they would
             be ~42 GB of pure inflation. Growler LASTZ takes them as fixed inputs. -->
        <data name="target_2bit" format="twobit" from_work_dir="work/ref.2bit" label="KegAlign target 2bit on ${on_string}">
            <filter>bundle_selector == "collection"</filter>
        </data>
        <data name="query_2bit" format="twobit" from_work_dir="work/query.2bit" label="KegAlign query 2bit on ${on_string}">
            <filter>bundle_selector == "collection"</filter>
        </data>
    </outputs>
    <tests>
        <test expect_num_outputs="1" expect_test_failure="true">
            <param name="target" value="hg38.chr20.chunk.fa.gz" ftype="fasta.gz"/>
            <param name="query" value="mm39.chr2.chunk.fa.gz" ftype="fasta.gz"/>
            <output name="kegalign_output" ftype="tgz">
                <assert_contents>
                    <has_archive_member path="galaxy/commands.json"/>
                </assert_contents>
            </output>
        </test>
    </tests>
    <help><![CDATA[
KegAlign is a scalable, GPU-accelerated system for computing pairwise WGA. KegAlign is based on the standard seed-filter-extend heuristic, in which the filtering stage dominates the runtime (e.g. 98% for human-mouse WGA), and is accelerated using GPU(s). KegAlign was designed as a faster replacement for lastz pairwise aligner.

**Using this tool**

.. class:: warningmark

This tool is the first part of a two-step process for generation of paiwrise alignments. The output of this tool is used as an input to **Batched LASTZ** tool. The *Batched LASTZ** can be found in the list of tool of this Galaxy instance.

**What it does**

KegAlign processes **Target** and **Query** sequences to identify highly similar regions where gapped extension will be performed to create actual alignments. The actual alignments are generated by **Batched LASTZ** that should be run on the output of this tool.

.. class:: infomark

Although this tool is only the first part of the alignment process all parameters related to generation of alignments are set **during this stage**.

**Scoring Options**

By default the HOXD70 substitution scores are used (from `Chiaromonte et al. 2002 <https://doi.org/10.1142/9789812799623_0012>`_)::

    bad_score          = X:-1000  # used for sub['X'][*] and sub[*]['X']
    fill_score         = -100     # used when sub[*][*] is not defined
    gap_open_penalty   =  400
    gap_extend_penalty =   30

         A     C     G     T
    A   91  -114   -31  -123
    C -114   100  -125   -31
    G  -31  -125   100  -114
    T -123   -31  -114    91

Matrix can be supplied as an input to **Read the substitution scores** parameter in *Scoring* section. Substitution matrix can be inferred from your data using another LASTZ-based tool (LASTZ_D: Infer substitution scores).

.. class:: infomark

From KegAlign 0.3.0, a supplied scoring file's ``bad_score`` and ``fill_score`` are honoured in the
gap-free extension stage as well as the gapped one. Earlier versions read both values but used them
only for gapped extension, so a custom file was interpreted two different ways within a single run.
Results from a custom scoring file are therefore not directly comparable with those from 0.2.2.14
or earlier.

**Output Options**

.. class:: infomark

The final format in which alignmnets will be generated by **Batched LASTZ** are set here.

The default output is a MAF alignment file. Other formats can be configured in *Output Options* section.  See LASTZ manual <https://lastz.github.io/lastz/#formats>`_ for description of possible formats.
    ]]></help>
    <expand macro="kegalign_citations"/>
</tool>
